# ── Editor / OS artifacts ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# ── Logs ─────────────────────────────────────────────────────────────────
*.log

# NOTE: config.js is tracked intentionally — it ships with an empty CLIENT_ID.
# Never paste a real Client ID into config.js and commit it.
# The setup wizard saves your Client ID safely in your browser's localStorage.
